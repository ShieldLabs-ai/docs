# Setup

- Create a project in the Dashboard
- Retrieve API keys (public, secret, webhook secret)
- Add the SDK to your web app
- Configure CSP and (optionally) custom domain
- Register a webhook and verify signatures
- Define rules & thresholds
